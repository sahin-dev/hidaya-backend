version: '3.8'

services:
  backend:
    build: ./
    env_file:
      - .env
    ports:
      - 5050:5050
    volumes:
      - logs:/app/logs
      - /app/node_modules # anonymous volume
      - ./:/app

volumes:
  logs:
